/**
 * GramAI site configuration — edit these values when going live.
 * Loaded before script.js on the landing page.
 */
window.GRAMAI_CONFIG = {
  GITHUB_REPO: 'TRUSHAKHACHARIYA/gramai-extension',
  SITE_URL: 'https://gramai-extension.vercel.app',

  // Set when Chrome Web Store listing is live:
  CHROME_STORE_URL: '',

  // Set when cloud server is deployed (Render, Railway, Fly.io):
  CLOUD_API_URL: '',

  // Auto-derived from CLOUD_API_URL if empty:
  WAITLIST_ENDPOINT: '',

  LEMONSQUEEZY_PRO_URL: 'https://gramai.lemonsqueezy.com/checkout/pro',
  LEMONSQUEEZY_TEAM_URL: 'https://gramai.lemonsqueezy.com/checkout/team',

  PLAUSIBLE_DOMAIN: 'gramai-extension.vercel.app',
  EARLY_ACCESS: true,
  EARLY_ACCESS_MESSAGE: 'Early access — Pro features free until official launch',
};
